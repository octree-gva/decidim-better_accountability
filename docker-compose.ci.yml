# Local GitLab CI parity (rspec). Chrome is required for system specs.
# Not for interactive development — use docker-compose.yml.
#
#   docker compose -f docker-compose.ci.yml run --rm rspec

name: better-accountability-ci

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: decidim_test
      POSTGRES_USER: decidim
      POSTGRES_PASSWORD: TEST-insecure_password

  redis:
    image: redis:latest

  rspec:
    image: ruby:3.4.7
    working_dir: /app
    volumes:
      - .:/app
    shm_size: "2gb"
    depends_on:
      - postgres
      - redis
    environment:
      DATABASE_HOST: postgres
      DATABASE_USERNAME: decidim
      DATABASE_PASSWORD: TEST-insecure_password
      DISABLED_DOCKER_COMPOSE: "true"
      DISABLE_SPRING: "1"
      TRAEFIK_REDIS_URL: redis://redis:6379/1
      SIMPLECOV: "1"
      CI: "1"
    command: >
      bash -lc "
        apt-get update -qq &&
        apt-get install -y -qq ca-certificates curl gnupg wget libvips42 &&
        wget -q -O /tmp/google-chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb &&
        apt-get install -y /tmp/google-chrome.deb || (apt-get -f install -y && apt-get install -y /tmp/google-chrome.deb) &&
        rm -f /tmp/google-chrome.deb &&
        curl -fsSL https://deb.nodesource.com/setup_22.x | bash - &&
        apt-get install -y -qq nodejs &&
        corepack enable &&
        corepack prepare yarn@1.22.22 --activate &&
        gem install bundler &&
        bundle config set path vendor &&
        RAILS_ENV=development bundle install -j $$(nproc) &&
        bundle exec rake test_app &&
        RAILS_ENV=test bundle exec rspec spec
      "
